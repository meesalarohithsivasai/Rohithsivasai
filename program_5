% Facts: student_grade(Student, Grade). student_grade(alice, 90). student_grade(bob, 45). student_grade(charlie, 60). student_grade(david, 85). % Rules passed(Student) :- student_grade(Student, Grade), Grade >= 50. failed(Student) :- student_grade(Student, Grade), Grade < 50. distinction(Student) :- student_grade(Student, Grade), Grade >= 80.

Queries to try:

1. Did Bob pass? ?- passed(bob).

2. Who got a distinction? ?- distinction(Who).

3. List everyone who failed. ?- failed(X).
